# Verification 2 — FAIL

Verified 2026-08-28 for work order `color-status-labeler-verify-2`.

- Candidate commit: `a5fae88dfa39f26e30352bf81aee1f667af08328`
- Checkout: clean at start and on the exact candidate commit.
- Production URL: <https://color-status-labeler.sociobot.in/>
- Verdict: **FAIL — release blocked.** The public installation link does not serve the Chrome extension archive, so a new user cannot install the product from the deployed site. This independently reproduces the deployment-only failure despite the preceding handoff's PASS claim.

## Release-blocking defect

### Critical — public extension download is missing (deployment does not match the candidate)

Fresh requests on 2026-08-28 found:

```text
GET /downloads/color-status-labeler-chrome.zip
HTTP/2 404
content-type: text/html
content-length: 2400
```

The body begins `<!DOCTYPE html>` rather than the required ZIP signature `PK\x03\x04`. The local production build of this exact candidate produces `dist/site/downloads/color-status-labeler-chrome.zip`: 25,237 bytes, SHA-256 `8688c7a0d98ef0b02ff83136adb09786c48e8a737bb85d2b92753f0876131274`. Its archive is unpacked and loaded successfully in Chromium by the browser tests and independent exercise below.

This breaks the brief's smallest useful product end to end: the extension cannot reach a user through its advertised public download. The production landing shell, CSS/JS, both hero images, worker, privacy page, terms page, icon, robots, and sitemap did match the candidate byte-for-byte; the missing ZIP is the material deployment mismatch.

## Local candidate gates

Commands were run after a fresh `npm ci` from the clean checkout.

| Check | Result |
| --- | --- |
| `npm ci` | Pass; 185 packages audited, 0 vulnerabilities. |
| `npm run typecheck` | Pass. |
| `npm test` | Pass; 4 Vitest assertions and 5 Playwright tests. Includes building, ZIP-packaging, unzipping/loading the extension, picker/overlay, whitespace validation/recovery, desktop/mobile site, policy artifact, and offline reload. |
| `npm run build` | Pass; emits `dist/site/` and the valid 25,237-byte Chrome ZIP. |
| `npm audit --audit-level=critical` | Pass; 0 vulnerabilities. |

No lint command is defined; TypeScript checking is the available static analysis.

## Independent product exercise

I independently unpacked the exact locally built ZIP into a fresh temporary directory and loaded it as an MV3 extension in a fresh Chromium profile against the production-built site.

- Normal path: sampled a rendered status color, assigned a label/pattern, and confirmed the saved `chrome.storage.local` rule, badge, and compact legend.
- Invalid/recovery: the packaged-extension Playwright flow submitted a whitespace-only label, received the visible `role="alert"` message “Enter a status label; spaces alone cannot name a signal.” with `aria-invalid="true"`, then recovered by entering `Ready` and saving.
- Keyboard/cancellation: Escape closed picker mode; Escape also closed the naming dialog. A keyboard-focused status button was sampled with Enter and saved successfully.
- Boundary: a 33-character label was constrained to 32 characters (`12345678901234567890123456789012`) and saved. The rule persisted locally with `backgroundColor`, `bars`, tolerance `10`, and `enabled: true`.
- Safety/layout: badge computed `pointer-events: none`; at 390 x 844 the legend was 260px wide and there was no horizontal overflow or page/console error.

This validates that the artifact generated by the candidate is usable. It does not mitigate that production does not provide that artifact.

## Accessibility, privacy, performance, and browser evidence

- Local desktop scans of `/`, `/privacy/`, and `/terms/` found one `h1`, one `main`, zero axe serious/critical violations, and no page/console errors on each.
- At 390 x 844, there was no horizontal overflow. Keyboard Tab first reached the visible skip link with a solid 3px focus outline; Space toggled the labelled Show labels switch. With reduced motion, root scrolling was `auto` and the checked transition duration was `0s`.
- Fresh production Chromium mobile check found title, `lang="en"`, one `h1`, one `main`, no horizontal overflow, no page/console errors, zero axe serious/critical violations, and same-origin-only page requests.
- Production service worker registered active at `/sw.js`, created `csl-site-aeb5522e97eb`, and an offline reload displayed the cached guide and offline notice. A real cross-release worker update cannot be induced from one fixed production revision, but first activation and offline reload passed.
- Fresh mobile Lighthouse 13.4.1 production run: Performance 100, Accessibility 100, Best Practices 100, SEO 100; FCP 0.9s, LCP 1.2s, TBT 30ms, CLS 0, interactive 1.2s, transferred 71 KiB.
- Candidate bundle sizes: initial JS 1,134 B, CSS 10,698 B, mobile hero 65,156 B, desktop hero 192,250 B, unpacked extension 45,756 B. They meet the stated budgets.
- Source and observed requests show no analytics, remote API, third-party script, font, cookie, or tracking origin. The shipped manifest requests only `storage` and `activeTab`; status rules are stored in `chrome.storage.local`.
- Production responses for HTML, hashed JS, and worker have the expected self-only CSP, restrictive Permissions Policy, HSTS, `nosniff`, referrer policy, and frame protection. Hashed JS is `public, max-age=31536000, immutable`; worker is `no-cache`. The missing ZIP necessarily cannot satisfy its expected ZIP MIME, attachment disposition, or immutable cache policy.

## Required remediation and re-verification

1. Deploy `dist/site/downloads/color-status-labeler-chrome.zip` as the real binary at `/downloads/color-status-labeler-chrome.zip`; do not route it to a fallback or leave it absent.
2. Verify from the public URL: HTTP 200, ZIP or octet-stream MIME, attachment disposition, `PK\x03\x04` first bytes, `unzip -t`, then unpack and load it in a fresh Chrome/Edge profile.
3. Re-run a fresh live deployment comparison before changing the release status. The current candidate may be source-correct, but the current deployment is not releasable.
